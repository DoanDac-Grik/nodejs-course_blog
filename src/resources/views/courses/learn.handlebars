
<div class="mt-4 d-flex justify-content-center">
    <div class="row ">
        
        <div class="col-lg ">
            <h2>{{course.name}}</h2>
            <iframe width="840" height="472" src="https://www.youtube.com/embed/{{course.videoId}}" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <p>{{course.description}}</p>
        </div>
    </div>
</div>